apiVersion: v1
kind: Service
metadata:
  name: my-backend-service
  labels:
    app: backend-restapp
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: backend-restapp
  ports:
  - name: http
    port: 8080    # ClusterIp sevice port
    targetPort: 8080    # Port on container